{
  "name": "hospital-doctor-admin",
  "version": "1.0.0",
  "description": "Hospital Doctor Admin System",
  "scripts": {
    "frontend": "cd frontendHospital/frontend && npm start",
    "backend": "cd server && npm start",
    "dev": "concurrently \"npm run frontend\" \"npm run backend\"",
    "install-all": "npm install && cd frontendHospital/frontend && npm install && cd ../../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}